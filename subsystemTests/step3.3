#!/bin/sh

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $(which $0))
. $THIS_SCRIPT_DIR/../envir.setup

# test functions
. $THIS_SCRIPT_DIR/test.include

echo "Expecting 11 roa filenames"
CMDLINE="../proto/query -s specs.1.3 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 11

echo "Expecting 11 roa filenames"
CMDLINE="../proto/query -s specs.3.3b -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 11

echo $0 PASS

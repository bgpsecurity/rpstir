#!/bin/sh

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $(which $0))
source $THIS_SCRIPT_DIR/../envir.setup

# test functions
source $THIS_SCRIPT_DIR/test.include

echo "Expecting 16 roa filenames"
CMDLINE="../proto/query -s specs.1.2 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 16

echo $0 PASS

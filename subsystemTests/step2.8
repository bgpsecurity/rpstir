#!/bin/bash

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $0)
. $THIS_SCRIPT_DIR/../envir.setup

. $RPKI_ROOT/trap_errors

# test functions
. $THIS_SCRIPT_DIR/test.include

echo "Expecting 3 roa filenames"
CMDLINE="../proto/query -s specs.2.6 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 3

echo "Expecting 2 roa filenames"
CMDLINE="../proto/query -s specs.2.8 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 2

echo $0 PASS

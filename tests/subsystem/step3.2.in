#!@SHELL_BASH@

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $0)
. $THIS_SCRIPT_DIR/../../etc/envir.setup

. $RPKI_ROOT/lib/util/trap_errors

# test functions
. $RPKI_ROOT/tests/test.include

echo "Expecting 12 roa filenames"
CMDLINE="query -s specs.1.2 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 12

echo $0 PASS

#!@SHELL_BASH@

@SETUP_ENVIRONMENT@

t4s_setup

@trap_errors@

# Due to the deprecation of the RPKIAllowStaleValidationChain option,
# this step became identical to the first half of step3.3.tap.in.
# Rather than run the same tests twice, this step has been completely
# gutted and will be removed in the future.

t4s_testcase "(test no longer needed)" 'true'

t4s_done

#!@SHELL_BASH@

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $0)
. $THIS_SCRIPT_DIR/../../etc/envir.setup

. $CONFIG_ROOT_DIR/lib/util/trap_errors

# test functions
. $CONFIG_ROOT_DIR/tests/test.include

echo "Expecting 11 roa filenames"
export @CONFIG_ENV_VAR@=specs.1.3.conf
CMDLINE="query -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 11

echo "Expecting 11 roa filenames"
export @CONFIG_ENV_VAR@=specs.3.3b.conf
CMDLINE="query -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 11

echo $0 PASS

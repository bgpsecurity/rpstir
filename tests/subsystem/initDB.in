#!@SHELL_BASH@

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $0)
. $THIS_SCRIPT_DIR/../../etc/envir.setup

. $RPKI_ROOT/lib/util/trap_errors

. $RPKI_ROOT/tests/test.include

echo About to clear database ...

# set up an initial database
run "rcli-x-t-y" rcli -x -t $THIS_SCRIPT_DIR/testcases -y
run "rcli-y-F" rcli -y -F $THIS_SCRIPT_DIR/testcases/C.cer

#!@SHELL_BASH@

# set environment variables if not set
THIS_SCRIPT_DIR=$(dirname $0)
. $THIS_SCRIPT_DIR/../../etc/envir.setup

. $CONFIG_ROOT_DIR/lib/util/trap_errors

# test functions
. $CONFIG_ROOT_DIR/tests/test.include

echo "Expecting 0 roa filenames"
CMDLINE="query -s specs.3.7 -t roa -d filename"
check_num_output_lines "$CMDLINE" "query client failed" 0

echo $0 PASS
